<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}DernekKapı{% endblock %}</title>
    <link rel="icon" type="image/png" href="{{ url_for('static', filename='assets/dernekkapi-icon-500x500.png') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #1e3a8a;
            --primary-light: #3b82f6;
            --secondary-color: #6b7280;
            --success-color: #047857;
            --warning-color: #b45309;
            --danger-color: #b91c1c;
            --dark-color: #111827;
            --light-color: #f9fafb;
            --border-color: #d1d5db;
            --sidebar-bg: #111827;
            --sidebar-hover: #1f2937;
            --card-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
            --transition: all 0.15s ease;
        }

        * {
            font-family: 'Inter', sans-serif;
        }

        body {
            background-color: #f9fafb;
            color: #374151;
            font-weight: 400;
            line-height: 1.6;
        }

        /* Sidebar Styles */
        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            height: 100vh;
            width: 250px;
            background-color: var(--sidebar-bg);
            border-right: 1px solid #374151;
            z-index: 1000;
            overflow-y: auto;
        }

        .sidebar .nav-link {
            color: #9ca3af;
            padding: 10px 16px;
            margin: 1px 8px;
            border-radius: 4px;
            transition: var(--transition);
            font-weight: 500;
            font-size: 0.9rem;
        }

        .sidebar .nav-link:hover {
            color: #ffffff;
            background-color: var(--sidebar-hover);
        }

        .sidebar .nav-link.active {
            color: #ffffff;
            background-color: var(--primary-color);
        }

        .sidebar .nav-link i {
            width: 16px;
            text-align: center;
            margin-right: 10px;
        }

        .sidebar-header {
            padding: 20px 16px;
            border-bottom: 1px solid #374151;
            margin-bottom: 20px;
        }

        .user-info {
            color: #ffffff;
            font-weight: 500;
            font-size: 0.9rem;
            padding: 8px 12px;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 6px;
            display: inline-block;
        }

        .sidebar-footer {
            border-top: 1px solid #374151;
            margin-top: auto;
        }

        .developed-by-link {
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            transition: opacity 0.2s ease;
        }

        .developed-by-link:hover {
            opacity: 0.8;
            text-decoration: none;
        }

        .developed-by-text {
            color: #9ca3af;
            font-size: 0.75rem;
            font-weight: 400;
        }

        .sidebar .nav-link.text-danger {
            border-top: 1px solid #374151;
            margin-top: 8px;
            padding-top: 12px;
        }

        .sidebar .nav-link.text-danger:hover {
            background-color: rgba(220, 53, 69, 0.1);
        }

                /* Main Content */
        .main-content {
            margin-left: 250px;
            padding: 20px;
            min-height: 100vh;
            width: calc(100vw - 250px);
            overflow-x: auto;
        }

        /* Mobile responsive */
        @media (max-width: 768px) {
            .sidebar {
                position: static;
                width: 100%;
                height: auto;
            }
            .main-content {
                margin-left: 0;
                width: 100%;
            }
        }

        /* Cards */
        .card {
            border: 1px solid var(--border-color);
            border-radius: 6px;
            box-shadow: var(--card-shadow);
            background: #ffffff;
        }

        .card-header {
            background-color: #f9fafb;
            border-bottom: 1px solid var(--border-color);
            border-radius: 6px 6px 0 0 !important;
            padding: 12px 16px;
        }

        .card-title {
            font-weight: 600;
            color: var(--dark-color);
            margin: 0;
            font-size: 1rem;
        }

        /* Buttons */
        .btn {
            border-radius: 4px;
            font-weight: 500;
            padding: 6px 12px;
            transition: var(--transition);
            border: 1px solid transparent;
            font-size: 0.9rem;
        }

        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }

        .btn-primary:hover {
            background-color: #1e40af;
            border-color: #1e40af;
        }

        .btn-success {
            background-color: var(--success-color);
            border-color: var(--success-color);
        }

        .btn-success:hover {
            background-color: #065f46;
            border-color: #065f46;
        }

        .btn-info {
            background-color: #0891b2;
            border-color: #0891b2;
        }

        .btn-info:hover {
            background-color: #0e7490;
            border-color: #0e7490;
        }

        .btn-outline-primary {
            color: var(--primary-color);
            border-color: var(--primary-color);
        }

        .btn-outline-primary:hover {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }

        .btn-outline-success {
            color: var(--success-color);
            border-color: var(--success-color);
        }

        .btn-outline-success:hover {
            background-color: var(--success-color);
            border-color: var(--success-color);
        }

        /* Alerts */
        .alert {
            border: 1px solid transparent;
            border-radius: 4px;
            padding: 10px 12px;
            margin-bottom: 12px;
            font-size: 0.9rem;
        }

        .alert-success {
            background-color: #d1fae5;
            border-color: #a7f3d0;
            color: #065f46;
        }

        .alert-danger {
            background-color: #fee2e2;
            border-color: #fecaca;
            color: #991b1b;
        }

        .alert-warning {
            background-color: #fef3c7;
            border-color: #fde68a;
            color: #92400e;
        }

        /* Forms */
        .form-control, .form-select {
            border: 1px solid var(--border-color);
            border-radius: 4px;
            padding: 8px 10px;
            transition: var(--transition);
            font-size: 0.9rem;
        }

        .form-control:focus, .form-select:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 1px rgba(30, 58, 138, 0.1);
        }

        .form-label {
            font-weight: 500;
            color: var(--dark-color);
            margin-bottom: 4px;
            font-size: 0.85rem;
        }

        /* Stats Cards */
        .stats-card {
            background: #ffffff;
            border: 1px solid var(--border-color);
            border-radius: 6px;
            padding: 16px;
            text-align: center;
            transition: var(--transition);
        }

        .stats-card:hover {
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        }

        .stats-card .icon {
            width: 40px;
            height: 40px;
            border-radius: 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 8px;
            font-size: 18px;
            color: white;
        }

        .stats-card.primary .icon {
            background-color: var(--primary-color);
        }

        .stats-card.success .icon {
            background-color: var(--success-color);
        }

        .stats-card.info .icon {
            background-color: #0891b2;
        }

        .stats-card.warning .icon {
            background-color: var(--warning-color);
        }

        .stats-number {
            font-size: 1.75rem;
            font-weight: 600;
            color: var(--dark-color);
            margin-bottom: 2px;
        }

        .stats-label {
            color: var(--secondary-color);
            font-weight: 500;
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Tables */
        .table {
            border-radius: 4px;
            overflow: hidden;
            border: 1px solid var(--border-color);
            font-size: 0.9rem;
        }

        .table thead th {
            background-color: #f9fafb;
            border: none;
            font-weight: 600;
            color: var(--dark-color);
            padding: 10px 12px;
            font-size: 0.85rem;
        }

        .table tbody td {
            padding: 10px 12px;
            border-color: var(--border-color);
            vertical-align: middle;
            font-size: 0.85rem;
        }

        .table tbody tr:hover {
            background-color: #f9fafb;
        }

        /* Badges */
        .badge {
            font-weight: 500;
            padding: 4px 8px;
            border-radius: 3px;
            font-size: 0.75rem;
        }

        /* Progress bars */
        .progress {
            height: 4px;
            border-radius: 2px;
            background-color: #e5e7eb;
        }

        .progress-bar {
            border-radius: 2px;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .sidebar {
                position: fixed;
                top: 0;
                left: -100%;
                z-index: 1000;
                width: 260px;
            }

            .sidebar.show {
                left: 0;
            }

            .main-content {
                padding: 16px;
            }

            .stats-number {
                font-size: 1.5rem;
            }
        }

        /* Loading spinner */
        .loading {
            display: inline-block;
            width: 14px;
            height: 14px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            border-top-color: #fff;
            animation: spin 1s ease-in-out infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        /* Page headers */
        .page-header {
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 12px;
            margin-bottom: 20px;
        }

        .page-header h1 {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--dark-color);
            margin: 0;
        }

        /* Breadcrumb */
        .breadcrumb {
            background: none;
            padding: 0;
            margin: 0;
            font-size: 0.85rem;
        }

        .breadcrumb-item + .breadcrumb-item::before {
            content: ">";
            color: var(--secondary-color);
        }

        .breadcrumb-item a {
            color: var(--primary-color);
            text-decoration: none;
        }

        .breadcrumb-item.active {
            color: var(--secondary-color);
        }

        /* Input groups */
        .input-group-text {
            background-color: #f9fafb;
            border-color: var(--border-color);
            color: var(--secondary-color);
            font-size: 0.9rem;
        }

        /* Dropdowns */
        .dropdown-menu {
            border: 1px solid var(--border-color);
            border-radius: 4px;
            box-shadow: var(--card-shadow);
            font-size: 0.9rem;
        }

        .dropdown-item {
            padding: 6px 12px;
            font-size: 0.85rem;
        }

        .dropdown-item:hover {
            background-color: #f9fafb;
        }
    </style>
    {% block extra_css %}{% endblock %}
</head>
<body>
    {% if session.user_type %}
    <!-- Sidebar -->
    <nav class="sidebar" id="sidebar">
                <div class="position-sticky d-flex flex-column" style="height: 100vh;">
                    <div class="sidebar-header">
                        <div class="text-center mb-3">
                            <img src="{{ url_for('static', filename='assets/dernekkapı-transparent-1200x500.png') }}"
                                 alt="DernekKapı Logo"
                                 class="img-fluid"
                                 style="max-width: 120px; height: auto;">
                        </div>
                        <div class="text-center">
                            <div class="user-info">
                                {% if session.user_type == 'admin' %}
                                    <i class="fas fa-shield-alt me-2"></i>Yönetim Paneli
                                {% else %}
                                    <i class="fas fa-building me-2"></i>{{ session.association_name }}
                                {% endif %}
                            </div>
                        </div>
                    </div>

                                        <ul class="nav flex-column flex-grow-1">
                        {% if session.user_type == 'admin' %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('admin.dashboard') }}">
                                    <i class="fas fa-tachometer-alt"></i>
                                    Dashboard
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('admin.users') }}">
                                    <i class="fas fa-users-cog"></i>
                                    Kullanıcı Yönetimi
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('admin.all_members') }}">
                                    <i class="fas fa-users"></i>
                                    Tüm Üyeler
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('admin.all_receipts') }}">
                                    <i class="fas fa-receipt"></i>
                                    Tüm Makbuzlar
                                </a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('dashboard.index') }}">
                                    <i class="fas fa-home"></i>
                                    Ana Sayfa
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('members.create') }}">
                                    <i class="fas fa-user-plus"></i>
                                    Yeni Üye Ekle
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('members.list') }}">
                                    <i class="fas fa-users"></i>
                                    Üye Listesi
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ url_for('dashboard.profile') }}">
                                    <i class="fas fa-user-cog"></i>
                                    Profil Ayarları
                                </a>
                            </li>
                        {% endif %}

                        <li class="nav-item mt-4 mb-2">
                            <a class="nav-link text-danger" href="{{ url_for('auth.logout') }}">
                                <i class="fas fa-sign-out-alt"></i>
                                Çıkış Yap
                            </a>
                        </li>
                    </ul>

                    <!-- Developed by section -->
                    <div class="sidebar-footer mt-auto pt-3 pb-3">
                        <div class="text-center">
                            <a href="https://cengel.studio" target="_blank" class="developed-by-link">
                                <small class="developed-by-text">Developed by</small>
                                <img src="{{ url_for('static', filename='assets/cengelstudio-logo.png') }}"
                                     alt="Cengel Studio"
                                     class="img-fluid rounded-circle ms-2"
                                     style="width: 24px; height: 24px; object-fit: cover;">
                            </a>
                        </div>
                    </div>
                </div>
            </nav>

            <!-- Main content -->
            <main class="main-content">
                <!-- Mobile menu button -->
                <div class="d-md-none mb-3">
                    <button class="btn btn-outline-primary btn-sm" type="button" data-bs-toggle="collapse" data-bs-target="#sidebar">
                        <i class="fas fa-bars"></i> Menü
                    </button>
                </div>

                {% with messages = get_flashed_messages(with_categories=true) %}
                    {% if messages %}
                        {% for category, message in messages %}
                            <div class="alert alert-{{ 'danger' if category == 'error' else category }} alert-dismissible fade show" role="alert">
                                <i class="fas fa-{{ 'exclamation-triangle' if category == 'error' else 'check-circle' if category == 'success' else 'info-circle' }} me-2"></i>
                                {{ message }}
                                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                            </div>
                        {% endfor %}
                    {% endif %}
                {% endwith %}

                {% block content %}{% endblock %}
            </main>
    {% else %}
        {% block auth_content %}{% endblock %}
    {% endif %}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Active link highlighting
        document.addEventListener('DOMContentLoaded', function() {
            const currentPath = window.location.pathname;
            const navLinks = document.querySelectorAll('.sidebar .nav-link');

            navLinks.forEach(link => {
                if (link.getAttribute('href') === currentPath) {
                    link.classList.add('active');
                }
            });

            // Mobile sidebar toggle
            const sidebarToggle = document.querySelector('[data-bs-toggle="collapse"]');
            if (sidebarToggle) {
                sidebarToggle.addEventListener('click', function() {
                    const sidebar = document.getElementById('sidebar');
                    sidebar.classList.toggle('show');
                });
            }

            // Close sidebar when clicking outside on mobile
            document.addEventListener('click', function(e) {
                const sidebar = document.getElementById('sidebar');
                const sidebarToggle = document.querySelector('[data-bs-toggle="collapse"]');

                if (window.innerWidth < 768 &&
                    !sidebar.contains(e.target) &&
                    !sidebarToggle.contains(e.target) &&
                    sidebar.classList.contains('show')) {
                    sidebar.classList.remove('show');
                }
            });
        });

        // Loading states for buttons
        document.addEventListener('submit', function(e) {
            const form = e.target;
            const submitBtn = form.querySelector('button[type="submit"]');

            if (submitBtn) {
                const originalText = submitBtn.innerHTML;
                submitBtn.innerHTML = '<span class="loading"></span> İşleniyor...';
                submitBtn.disabled = true;

                setTimeout(() => {
                    submitBtn.innerHTML = originalText;
                    submitBtn.disabled = false;
                }, 3000);
            }
        });
    </script>
    {% block extra_js %}{% endblock %}
</body>
</html>
